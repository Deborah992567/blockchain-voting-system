[pytest]
# Discover tests whether pytest is run from the repo root or inside `backend/`
testpaths = tests backend/tests
python_files = test_*.py
addopts = -q
filterwarnings = ignore::DeprecationWarning
